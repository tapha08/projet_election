import { CandidatPartiColorPipe } from './candidat-parti-color.pipe';

describe('CandidatPartiColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CandidatPartiColorPipe();
    expect(pipe).toBeTruthy();
  });
});
